# Backend API URL (for Vercel deployment, set this to your Render backend URL, e.g. https://attendance-api.onrender.com/api/v1)
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# JWT Secret - MUST match the backend's JWT_SECRET exactly
JWT_SECRET=super-secret-key-for-attendance-app
